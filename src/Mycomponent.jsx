function MyComponent({ name, age }) {
    return (
        <div>
            <h2>자식 컴포넌트</h2>
            <p>이름: {name}</p>
            <p>나이: {age}</p>
        </div>
    );
}

export default MyComponent;